#input 语句读取用户输入的数字
#读取用户输入的数字储存在变量a
a = input（）
777 #输入数值
print a #打印变量a的内容
777 #输出结果


#如果2大于1，为true（真）打印第一条文本忽略第二条，为false（假）打印第二条忽略第一条
if 2 > 1:
    print('2 > 1 is True')
else:
    print('2 > 1 is False')

#list 创建一个列表 用list[]来读取列表信息从0开始
list = 1,2,3,'Q'
list[3] #例如

#引号作用
'Hello' + 'World'
'Hello' + ':' + 'World'
